machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull sabayon/builder-amd64
    - docker export $(docker run -i -t sabayon/builder-amd64 true && docker ps -aq | xargs echo | cut -d ' ' -f 1) | docker import - sabayon/builder-amd64-squashed

test:
  override:
    - docker run sabayon/builder-amd64-squashed true

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push sabayon/builder-amd64-squashed

